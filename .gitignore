# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]

# Spyder project settings
.spyderproject
.spyproject

# virtual environment - this could be a dir or a symlink to a path
# like ~/.local/virtualenvs/my_venv/
virtualenv
virtualenv/

# local configuration files
app/etc/*.local.conf

# Database. Journal is a temporary file which is created when working with
# the db.
*.db
*.sqlite
*-journal

# Variable files. But include `sample` dir.
# These may be output from scripts or may be input for other scripts to
# run.
!app/var/sample

app/var/*.json
app/var/*/*.json

app/var/*.csv
app/var/*/*.csv

app/var/*.txt
app/var/*/*.txt
app/var/lib/*/*.txt

# Proper log formatted files.
app/var/log/*/*.log
# Text files using for logging.
app/var/log/*/*.txt
